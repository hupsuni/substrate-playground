{
  "name": "@parity/theia-github-authentication-plugin",
  "version": "0.0.1",
  "files": [
    "src"
  ],
  "activationEvents": [
    "*"
  ],
  "devDependencies": {
    "@theia/plugin": "1.11.0",
    "@theia/plugin-packager": "0.0.1-1594417672",
    "rimraf": "2.6.2",
    "typescript-formatter": "7.2.2",
    "typescript": "2.9.2",
    "uuid": "^8.0.0"
  },
  "scripts": {
    "prepare": "yarn run clean && yarn run build",
    "clean": "rimraf lib",
    "format-code": "tsfmt -r",
    "watch": "tsc -watch",
    "compile": "tsc",
    "build": "yarn run format-code && yarn run compile && theia-plugin pack"
  },
  "engines": {
    "theiaPlugin": "next"
  },
  "theiaPlugin": {
    "backend": "lib/github-authentication-plugin.js"
  }
}
